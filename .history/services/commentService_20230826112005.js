const Comment = require('../models/comment');

// @permission should authorizated
exports.creatService = (req, res, next) => {
    // userId
    const userId =  user._id;
    const postId = await Po
    req.body.userId = userId;
}
